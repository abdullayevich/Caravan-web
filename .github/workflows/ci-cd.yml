# This workflow for .NET project is CI and CD.
# Not completely

name: CI/CD

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]


jobs:
  build:
  
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
        
    - name: Restore dependencies
      run: dotnet restore
      
    - name: Build
      run: dotnet build --configuration Release --no-restore
      
    - name: Test
      run: dotnet test --no-build --configuration Release --verbosity normal
      
    - name: Package Application
      run: dotnet publish -c Release -o publish
      
    - name: Copy application to Lightsail instance
      uses: appleboy/scp-action@v3
      with:
        host: 3.76.99.135
        username: nurmamatovnodirbek43@gmail.com
        password: Qahhor2002@
        source: publish
        target: /home/<username>/app
    - name: Start application on Lightsail instance
      uses: appleboy/ssh-action@v3
      with:
        host: 3.76.99.135
        username: nurmamatovnodirbek43@gmail.com
        password: Qahhor2002@
        script: |
          cd /home/<username>/app
          dotnet Caravan.Web.dll
